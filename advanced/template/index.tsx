import ReactDOM from "react-dom";
import { TemplateBoard } from "./components/board";
import React from "react";

const Root = document.getElementById("board-app");
ReactDOM.render(<TemplateBoard />, Root);
